PORT=5000
DEBUG=False
FLASK_ENV=development

# Security (REQUIRED for production)
JWT_SECRET=hv84U4fDCo5k3x36fIMcTyvu6NFrAzwQ
ADMIN_BOOTSTRAP_KEY=shadow@123
ADMIN_EMAILS=admin@example.com

# CORS — comma-separated frontend URLs (production)
# Local dev:
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
# Production (solutionservices.co.in):
# ALLOWED_ORIGINS=https://solutionservices.co.in,https://www.solutionservices.co.in

JWT_EXPIRE_HOURS=168

# SQLite database file (optional; default: backend/urban_services.db)
# Fly.io with persistent volume:
# DATABASE_PATH=/data/urban_services.db

# Legacy MySQL settings (not used — app uses SQLite)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=solution_services

# Legacy admin JWT login (/api/auth/login)
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=change-me

# Optional notifications
EMAIL_HOST=
EMAIL_PORT=587
EMAIL_USER=
EMAIL_PASS=
NOTIFICATION_EMAIL=
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_WHATSAPP_FROM=
TWILIO_WHATSAPP_TO=
